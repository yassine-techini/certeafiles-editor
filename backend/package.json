{
  "name": "certeafiles-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create certeafiles-db",
    "db:migrate": "wrangler d1 execute certeafiles-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute certeafiles-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "yjs": "^13.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
